// @flow

describe('<DocumentCursorComponent/>', (): void => {
  test('Should highlight selected position within a textarea', (): void => {

  })

  test('Should highlight a selected range', (): void => {

  })

  test('Should be able to highlight range across multiple document components', (): void => {

  })
})